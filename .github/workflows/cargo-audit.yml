name: Cargo Audit

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 0 * * 1'

jobs:
  audit:
    uses: drasi-project/.github/.github/workflows/cargo-audit.yaml@main
